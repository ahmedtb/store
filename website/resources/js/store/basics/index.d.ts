export { };
declare global {
    interface Window {
        localization: any;
    }

}
window.localization = window.localization || {};
window.user = window.user || {};
window.Echo = window.Echo || {};


